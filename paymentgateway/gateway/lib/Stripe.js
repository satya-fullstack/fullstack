'use strict';

function Stripe (credentials) {
    //TODO
}

Stripe.prototype.submitTransaction = function submitTransaction (params) {
    //TODO
    console.log(JSON.stringify(params));
};

module.exports = Stripe;